import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dawa.examportal',
  appName: 'Dawa College Exam Portal',
  webDir: 'dist'
};

export default config;
